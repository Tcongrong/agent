<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.chat.mapper.ChatMessageMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.demo.chat.entity.ChatMessage">
        <id column="id" property="id" />
        <result column="session_id" property="sessionId" />
        <result column="user_id" property="userId" />
        <result column="role" property="role" />
        <result column="content" property="content" />
        <result column="created_at" property="createdAt" />
    </resultMap>
    
    <!-- 根据ID查询消息 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, session_id, user_id, role, content, created_at
        FROM chat_message
        WHERE id = #{id}
    </select>
    
    <!-- 根据会话ID查询消息列表 -->
    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT id, session_id, user_id, role, content, created_at
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>
    
    <!-- 插入消息 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (session_id, user_id, role, content, created_at)
        VALUES (#{sessionId}, #{userId}, #{role}, #{content}, #{createdAt})
    </insert>
    
    <!-- 根据会话ID删除所有消息 -->
    <delete id="deleteBySessionId">
        DELETE FROM chat_message
        WHERE session_id = #{sessionId}
    </delete>
    
    <!-- 统计会话中的消息数量 -->
    <select id="countBySessionId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM chat_message
        WHERE session_id = #{sessionId}
    </select>
</mapper>